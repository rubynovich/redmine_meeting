- if can_create_comments?(@object)
  %p
  .contextual
    = link_to l(:button_add), {controller: 'meeting_comments', action: 'new', meeting_comment: {meeting_container_id: @object.id, meeting_container_type: @object.class}}, remote: true

%h3= l(:label_meeting_comment_plural)

= render partial: 'meeting_comments/history', locals: {meeting_container: @object}
